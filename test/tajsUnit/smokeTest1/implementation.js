(function(root, undefined) {
    var lib = {};
    lib.formatColumn = function(list, symbol, precision, thousand, decimal, format) {};
    if (typeof exports !== 'undefined') {} else if (typeof define === 'function' && define.amd) {} else {
        root['accounting'] = lib;
    }
}(this));