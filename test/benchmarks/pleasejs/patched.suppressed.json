{
  "suppressions": [
    {
      "path": "Please.make_scheme(obj,obj)",
      "comment": "Precision-loss, cannot prove that it is Array<A> | Array<B>, only that it is Array<A|B>. (And if changed to Array<A|B>, another precision loss would put a spurious undefined in there)."
    },
    {
      "path": "Please.make_scheme(obj)",
      "comment": "Precision-loss, cannot prove that it is Array<A> | Array<B>, only that it is Array<A|B>. (And if changed to Array<A|B>, another precision loss would put a spurious undefined in there)."
    },
    {
      "path": "Please.make_color(obj).[numberIndexer]",
      "comment": "Spurious undefined in RGB."
    },
    {
      "path": "Please.make_color().<>.[numberIndexer]",
      "comment": "Spurious undefined in RGB."
    },
    {
      "path": "Please.make_scheme(obj,undefined)",
      "comment": "Precision-loss, cannot prove that it is Array<A> | Array<B>, only that it is Array<A|B>. (And if changed to Array<A|B>, another precision loss would put a spurious undefined in there)."
    },
    {
      "path": "Please.make_color()",
      "comment": "Precision-loss, cannot prove that it is Array<A> | Array<B>, only that it is Array<A|B>. (And if changed to Array<A|B>, another precision loss would put a spurious undefined in there)."
    },
    {
      "path": "Please.make_color(undefined)",
      "comment": "Precision-loss, cannot prove that it is Array<A> | Array<B>, only that it is Array<A|B>. (And if changed to Array<A|B>, another precision loss would put a spurious undefined in there)."
    },
    {
      "path": "Please.make_color(obj)",
      "comment": "Precision-loss, cannot prove that it is Array<A> | Array<B>, only that it is Array<A|B>. (And if changed to Array<A|B>, another precision loss would put a spurious undefined in there)."
    },
    {
      "path": "Please.make_scheme(obj,obj).[numberIndexer]",
      "comment": "Spurious undefined in RGB"
    },
    {
      "path": "Please.make_scheme(obj).[numberIndexer]",
      "comment": "Spurious undefined in RGB"
    },
    {
      "path": "Please.make_scheme(obj,undefined).[numberIndexer]",
      "comment": "Spurious undefined in RGB"
    },
    {
      "path": "Please.make_color(undefined).[numberIndexer]",
      "comment": "Spurious undefined in RGB"
    },
    {
      "path": "Please.make_color().[numberIndexer]",
      "comment": "Spurious undefined in RGB."
    }
  ]
}